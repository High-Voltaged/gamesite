export enum Lengths {

    maxEmailLength = 320,

    minPasswordLength = 8,
    maxPasswordLength = 72,

    minUsernameLength = 3,
    maxUsernameLength = 16,

    minAge = 13,
    maxAge = 99,

    minBioLength = 10, 
    maxBioLength = 5000, // I don't know yet

}